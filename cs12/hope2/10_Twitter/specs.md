3. Bulletin Board App

- Posts displayed as cards, sorted by date last modified (most recent first)

- Each post has a title, body text, author name, timestamp, and a comment count badge

- Users can add a new post via a modal form (title + body + author)

-  Comments can be added to any post; adding a comment bumps the post's modified date (re-sorts it)