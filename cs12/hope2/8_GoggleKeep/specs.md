1. Todo List (Google Keep–style)

- Display tasks as cards in a responsive grid or list layout
- Each task has a title, optional note, and a checkbox to mark complete
- Completed tasks are visually struck through and moved to a "Done" section
- Add tasks via an input bar at the top; delete tasks with a trash icon
- Support color labels per card (at least 4–5 colors)